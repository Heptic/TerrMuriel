if y < p1.y and y < p2.y {
    top = id;
}
if y > p1.y and y > p2.y {
    bot = id;
}
if top != id and bot != id midd = id;
/*
if y > p1.y and y < p2.x {
    midd = id;
}
if y < p1.y and y > p2.x {
    midd = id;
}
